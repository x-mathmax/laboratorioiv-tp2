import { EstadoColorPipe } from './estado-color.pipe';

describe('EstadoColorPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoColorPipe();
    expect(pipe).toBeTruthy();
  });
});
