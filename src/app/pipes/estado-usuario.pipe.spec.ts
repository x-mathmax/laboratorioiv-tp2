import { EstadoUsuarioPipe } from './estado-usuario.pipe';

describe('EstadoUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
